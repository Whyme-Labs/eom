# Streamlit Cloud reads requirements.txt directly.
# Keep this minimal — the eom package itself is installed from the local repo.
streamlit>=1.35
pydantic>=2.5
markdown-it-py>=3.0
jinja2>=3.1
tiktoken>=0.7
click>=8.1
rich>=13.7
httpx>=0.27
